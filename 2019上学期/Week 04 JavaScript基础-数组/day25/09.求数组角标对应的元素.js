// 9.定义一个整数数组，包含元素[1,3,4,7,8]，获取数组中数组角标和对应的元素都为偶数的元素与个数，并在控制台打印输出。

//       0  1  2  3  4  
var a = [1, 3, 4, 7, 8,];
var a_long = a.length;

for (var i = 1; i < a_long; i++) {
    // 求出数组中元素是偶数的 && 元素下标是偶数的
    if (a[i] % 2 == 0 && i % 2 == 0) {
        console.log(a[i])
    }
}