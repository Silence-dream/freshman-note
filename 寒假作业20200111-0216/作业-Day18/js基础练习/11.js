var a = [];
for (var i = 0; i < 10; i++) {
    a[i] = function () {
        console.log(i);
    };
}

a[6](); // 10 因为循环之中使用的是var定义变量在全局范围之内都可以被访问，每一次循环都会让后面的值覆盖掉前面的值
a[7](); // 10 
a[8](); // 10 
a[9](); // 10 